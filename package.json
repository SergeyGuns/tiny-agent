{
  "name": "tiny-agent",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/index.js",
    "tui": "npx tsc && node dist/agent.js",
    "dev": "npx tsc && node dist/development_framework.js",
    "clean": "node -e \"const fs = require('fs'); ['dist', 'session.log'].forEach(p => fs.existsSync(p) && fs.rmSync(p, {recursive: true}))\""
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
